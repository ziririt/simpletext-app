Auto-replace rules can now live on a single note: when saving from Find & Replace, choose between all notes and this note only. Tidy now turns spaced dashes ( — ) into colons ( : ) by default, and body text uses zero letter spacing so the system font looks the way it should.
